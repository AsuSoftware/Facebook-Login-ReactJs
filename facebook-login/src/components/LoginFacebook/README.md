# LoginFacebook

<!-- STORY -->

## Introduction

LoginFacebook is an easy-to-use component.

## Usage

```javascript
import { LoginFacebook } from 'some-package-name';
```

## Example use

```javascript
const myPage = props => {
  return (
    <main>
      <LoginFacebook />
    </main>
  );
};
```

## Properties

- `className` - is the class name of the component

| propName  | propType | defaultValue | isRequired |
| --------- | -------- | ------------ | ---------- |
| className | string   | -            | -          |
